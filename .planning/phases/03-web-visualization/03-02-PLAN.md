---
phase: 03-web-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/templates/index.html
  - src/web/static/styles.css
  - src/web/static/app.js
  - src/web/static/game-board.js
  - src/web/static/metrics-chart.js
autonomous: true

must_haves:
  truths:
    - "HTML template renders split view layout"
    - "CSS provides Tetris-appropriate styling"
    - "JavaScript files have correct module structure"
    - "Chart.js and Canvas APIs are set up correctly"
  artifacts:
    - path: "src/web/templates/index.html"
      provides: "Dashboard HTML with split view layout"
      contains: "game-board"
    - path: "src/web/static/styles.css"
      provides: "Tetris-themed styling with responsive layout"
    - path: "src/web/static/app.js"
      provides: "Main application entry point"
    - path: "src/web/static/game-board.js"
      provides: "Canvas-based Tetris board renderer"
      exports: ["GameBoard"]
    - path: "src/web/static/metrics-chart.js"
      provides: "Chart.js metrics visualization"
      exports: ["MetricsChart"]
  key_links:
    - from: "src/web/templates/index.html"
      to: "src/web/static/app.js"
      via: "script tag"
      pattern: "src=\"/static/app.js\""
    - from: "src/web/static/app.js"
      to: "src/web/static/game-board.js"
      via: "import or script loading"
      pattern: "GameBoard"
---

<objective>
Create frontend static assets: HTML template, CSS styling, and JavaScript module structure.

Purpose: Build the visual layer of the dashboard with split view layout (game board + metrics), Tetris-themed styling, and JavaScript structure for Canvas rendering and Chart.js charts.

Output: Complete frontend assets ready to be wired to WebSocket data in Plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-visualization/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML template with split view layout</name>
  <files>src/web/templates/index.html</files>
  <action>
Create `src/web/templates/index.html` with complete dashboard layout:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ML Tetris Trainer</title>
    <link rel="stylesheet" href="/static/styles.css">
    <!-- Chart.js from CDN -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
</head>
<body>
    <header>
        <h1>ML Tetris Trainer</h1>
        <div id="status-indicator" class="status stopped">
            <span class="status-dot"></span>
            <span id="status-text">Stopped</span>
        </div>
    </header>

    <main class="dashboard">
        <!-- Left panel: Game board -->
        <section class="panel game-panel">
            <h2>Game Board</h2>
            <div class="game-container">
                <canvas id="game-board" width="300" height="600"></canvas>
            </div>
            <div class="game-info">
                <div class="info-item">
                    <span class="label">Score:</span>
                    <span id="current-score">0</span>
                </div>
                <div class="info-item">
                    <span class="label">Lines:</span>
                    <span id="lines-cleared">0</span>
                </div>
                <div class="info-item">
                    <span class="label">Episode:</span>
                    <span id="episode-count">0</span>
                </div>
            </div>
        </section>

        <!-- Right panel: Metrics and controls -->
        <section class="panel metrics-panel">
            <h2>Training Metrics</h2>

            <div class="chart-container">
                <canvas id="metrics-chart"></canvas>
            </div>

            <div class="training-stats">
                <div class="stat-item">
                    <span class="label">Timesteps:</span>
                    <span id="timesteps">0</span>
                </div>
                <div class="stat-item">
                    <span class="label">Avg Reward:</span>
                    <span id="avg-reward">0.00</span>
                </div>
                <div class="stat-item">
                    <span class="label">Best Lines:</span>
                    <span id="best-lines">0</span>
                </div>
            </div>

            <div class="controls">
                <button id="btn-start" class="btn btn-start">Start Training</button>
                <button id="btn-stop" class="btn btn-stop" disabled>Stop Training</button>
            </div>

            <div class="config-section">
                <h3>Configuration</h3>
                <div class="config-item">
                    <label for="target-lines">Target Lines:</label>
                    <input type="number" id="target-lines" value="10" min="1" max="1000">
                </div>
                <div class="config-item">
                    <label for="max-timesteps">Max Timesteps:</label>
                    <input type="number" id="max-timesteps" value="100000" min="1000" step="1000">
                </div>
            </div>
        </section>
    </main>

    <footer>
        <p>ML Tetris Trainer - Real-time AI Training Visualization</p>
    </footer>

    <!-- Load scripts in order -->
    <script src="/static/game-board.js"></script>
    <script src="/static/metrics-chart.js"></script>
    <script src="/static/app.js"></script>
</body>
</html>
```

Key elements:
- VIS-01: Web interface with proper HTML structure
- VIS-03: Status indicator in header (running/paused/stopped)
- VIS-04: Split view with game-panel and metrics-panel
- TRAIN-02/03: Start/stop buttons
- TRAIN-05: Progress display (episode, score, lines)
- TRAIN-06: Chart canvas for metrics
  </action>
  <verify>
    ```bash
    grep -q "game-board" src/web/templates/index.html && grep -q "metrics-chart" src/web/templates/index.html && echo "HTML OK"
    ```
  </verify>
  <done>
    index.html contains split view layout with game board canvas, metrics chart canvas, controls, and status indicator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CSS styles with Tetris theme</name>
  <files>src/web/static/styles.css</files>
  <action>
Create `src/web/static/styles.css` with Tetris-themed styling:

```css
/* Reset and base styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --bg-dark: #1a1a2e;
    --bg-panel: #16213e;
    --accent: #00d4ff;
    --accent-dim: #0077b6;
    --text: #e8e8e8;
    --text-dim: #a0a0a0;
    --success: #00ff00;
    --warning: #ffd700;
    --danger: #ff4444;

    /* Tetris piece colors */
    --piece-i: #00d4ff;
    --piece-o: #ffd700;
    --piece-t: #9400d3;
    --piece-s: #00ff00;
    --piece-z: #ff0000;
    --piece-j: #0000ff;
    --piece-l: #ff8c00;
}

body {
    font-family: 'Segoe UI', system-ui, sans-serif;
    background: var(--bg-dark);
    color: var(--text);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

/* Header */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background: var(--bg-panel);
    border-bottom: 2px solid var(--accent);
}

header h1 {
    font-size: 1.5rem;
    color: var(--accent);
}

/* Status indicator */
.status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    background: rgba(0, 0, 0, 0.3);
}

.status-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    animation: pulse 2s infinite;
}

.status.stopped .status-dot { background: var(--danger); }
.status.running .status-dot { background: var(--success); }
.status.paused .status-dot { background: var(--warning); }

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}

/* Dashboard layout */
.dashboard {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    padding: 2rem;
    flex: 1;
}

@media (max-width: 900px) {
    .dashboard {
        grid-template-columns: 1fr;
    }
}

/* Panels */
.panel {
    background: var(--bg-panel);
    border-radius: 12px;
    padding: 1.5rem;
    border: 1px solid rgba(0, 212, 255, 0.2);
}

.panel h2 {
    color: var(--accent);
    margin-bottom: 1rem;
    font-size: 1.2rem;
    border-bottom: 1px solid rgba(0, 212, 255, 0.3);
    padding-bottom: 0.5rem;
}

/* Game panel */
.game-container {
    display: flex;
    justify-content: center;
    margin-bottom: 1rem;
}

#game-board {
    border: 3px solid var(--accent);
    border-radius: 4px;
    background: #0a0a15;
}

.game-info {
    display: flex;
    justify-content: space-around;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.3);
    border-radius: 8px;
}

.info-item, .stat-item {
    text-align: center;
}

.info-item .label, .stat-item .label {
    display: block;
    color: var(--text-dim);
    font-size: 0.8rem;
    margin-bottom: 0.25rem;
}

.info-item span:not(.label), .stat-item span:not(.label) {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--accent);
}

/* Metrics panel */
.chart-container {
    height: 250px;
    margin-bottom: 1rem;
}

.training-stats {
    display: flex;
    justify-content: space-around;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.3);
    border-radius: 8px;
    margin-bottom: 1rem;
}

/* Controls */
.controls {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.btn {
    flex: 1;
    padding: 1rem;
    font-size: 1rem;
    font-weight: bold;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s;
}

.btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.btn-start {
    background: var(--success);
    color: #000;
}

.btn-start:hover:not(:disabled) {
    background: #00cc00;
    transform: translateY(-2px);
}

.btn-stop {
    background: var(--danger);
    color: #fff;
}

.btn-stop:hover:not(:disabled) {
    background: #ff2222;
    transform: translateY(-2px);
}

/* Config section */
.config-section {
    background: rgba(0, 0, 0, 0.3);
    padding: 1rem;
    border-radius: 8px;
}

.config-section h3 {
    color: var(--text-dim);
    font-size: 0.9rem;
    margin-bottom: 1rem;
}

.config-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem;
}

.config-item label {
    color: var(--text-dim);
}

.config-item input {
    width: 120px;
    padding: 0.5rem;
    background: var(--bg-dark);
    border: 1px solid var(--accent-dim);
    border-radius: 4px;
    color: var(--text);
    font-size: 1rem;
}

.config-item input:focus {
    outline: none;
    border-color: var(--accent);
}

/* Footer */
footer {
    text-align: center;
    padding: 1rem;
    color: var(--text-dim);
    font-size: 0.8rem;
    border-top: 1px solid rgba(0, 212, 255, 0.2);
}
```
  </action>
  <verify>
    ```bash
    test -f src/web/static/styles.css && grep -q "game-board" src/web/static/styles.css && echo "CSS OK"
    ```
  </verify>
  <done>
    styles.css provides complete Tetris-themed styling with responsive split view layout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create JavaScript modules for game board and metrics chart</name>
  <files>
    src/web/static/game-board.js
    src/web/static/metrics-chart.js
    src/web/static/app.js
  </files>
  <action>
Create three JavaScript files:

1. `src/web/static/game-board.js` - Canvas-based Tetris renderer:
```javascript
/**
 * GameBoard - Canvas-based Tetris board renderer
 * Renders board state received from WebSocket as colored cells
 */
class GameBoard {
    constructor(canvasId, rows = 20, cols = 10) {
        this.canvas = document.getElementById(canvasId);
        this.ctx = this.canvas.getContext('2d');
        this.rows = rows;
        this.cols = cols;
        this.cellSize = this.canvas.width / cols;

        // Tetris piece colors (0 = empty, 1-7 = pieces I, O, T, S, Z, J, L)
        this.colors = {
            0: '#0a0a15',  // Empty
            1: '#00d4ff',  // I - Cyan
            2: '#ffd700',  // O - Yellow
            3: '#9400d3',  // T - Purple
            4: '#00ff00',  // S - Green
            5: '#ff0000',  // Z - Red
            6: '#0000ff',  // J - Blue
            7: '#ff8c00',  // L - Orange
        };
    }

    /**
     * Render board state to canvas
     * @param {number[][]} boardState - 2D array of piece IDs (20 rows x 10 cols)
     */
    render(boardState) {
        // Clear canvas
        this.ctx.fillStyle = this.colors[0];
        this.ctx.fillRect(0, 0, this.canvas.width, this.canvas.height);

        if (!boardState || !boardState.length) return;

        // Draw each cell
        for (let row = 0; row < Math.min(this.rows, boardState.length); row++) {
            for (let col = 0; col < Math.min(this.cols, boardState[row].length); col++) {
                const cellValue = boardState[row][col];
                if (cellValue > 0) {
                    this.drawCell(row, col, cellValue);
                }
            }
        }

        // Draw grid lines
        this.drawGrid();
    }

    /**
     * Draw a single cell with 3D effect
     */
    drawCell(row, col, pieceId) {
        const x = col * this.cellSize;
        const y = row * this.cellSize;
        const size = this.cellSize - 1;
        const color = this.colors[pieceId] || this.colors[1];

        // Main fill
        this.ctx.fillStyle = color;
        this.ctx.fillRect(x, y, size, size);

        // Highlight (top-left)
        this.ctx.fillStyle = 'rgba(255, 255, 255, 0.3)';
        this.ctx.fillRect(x, y, size, 3);
        this.ctx.fillRect(x, y, 3, size);

        // Shadow (bottom-right)
        this.ctx.fillStyle = 'rgba(0, 0, 0, 0.3)';
        this.ctx.fillRect(x, y + size - 3, size, 3);
        this.ctx.fillRect(x + size - 3, y, 3, size);
    }

    /**
     * Draw subtle grid lines
     */
    drawGrid() {
        this.ctx.strokeStyle = 'rgba(0, 212, 255, 0.1)';
        this.ctx.lineWidth = 0.5;

        for (let i = 0; i <= this.cols; i++) {
            this.ctx.beginPath();
            this.ctx.moveTo(i * this.cellSize, 0);
            this.ctx.lineTo(i * this.cellSize, this.canvas.height);
            this.ctx.stroke();
        }

        for (let i = 0; i <= this.rows; i++) {
            this.ctx.beginPath();
            this.ctx.moveTo(0, i * this.cellSize);
            this.ctx.lineTo(this.canvas.width, i * this.cellSize);
            this.ctx.stroke();
        }
    }

    /**
     * Clear the board (show empty state)
     */
    clear() {
        this.ctx.fillStyle = this.colors[0];
        this.ctx.fillRect(0, 0, this.canvas.width, this.canvas.height);
        this.drawGrid();
    }
}
```

2. `src/web/static/metrics-chart.js` - Chart.js metrics visualization:
```javascript
/**
 * MetricsChart - Chart.js wrapper for real-time training metrics
 */
class MetricsChart {
    constructor(canvasId) {
        this.ctx = document.getElementById(canvasId).getContext('2d');
        this.maxPoints = 100;  // Keep last 100 data points

        this.chart = new Chart(this.ctx, {
            type: 'line',
            data: {
                labels: [],
                datasets: [
                    {
                        label: 'Episode Reward',
                        data: [],
                        borderColor: '#00d4ff',
                        backgroundColor: 'rgba(0, 212, 255, 0.1)',
                        tension: 0.1,
                        fill: true,
                    },
                    {
                        label: 'Lines Cleared',
                        data: [],
                        borderColor: '#00ff00',
                        backgroundColor: 'rgba(0, 255, 0, 0.1)',
                        tension: 0.1,
                        fill: true,
                        yAxisID: 'y1',
                    }
                ]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                animation: { duration: 0 },  // Disable for performance
                interaction: {
                    mode: 'index',
                    intersect: false,
                },
                plugins: {
                    legend: {
                        labels: { color: '#e8e8e8' }
                    }
                },
                scales: {
                    x: {
                        title: {
                            display: true,
                            text: 'Episode',
                            color: '#a0a0a0'
                        },
                        ticks: { color: '#a0a0a0' },
                        grid: { color: 'rgba(255, 255, 255, 0.1)' }
                    },
                    y: {
                        type: 'linear',
                        display: true,
                        position: 'left',
                        title: {
                            display: true,
                            text: 'Reward',
                            color: '#00d4ff'
                        },
                        ticks: { color: '#00d4ff' },
                        grid: { color: 'rgba(255, 255, 255, 0.1)' }
                    },
                    y1: {
                        type: 'linear',
                        display: true,
                        position: 'right',
                        title: {
                            display: true,
                            text: 'Lines',
                            color: '#00ff00'
                        },
                        ticks: { color: '#00ff00' },
                        grid: { drawOnChartArea: false }
                    }
                }
            }
        });
    }

    /**
     * Add new data point to chart
     * @param {number} episode - Episode number
     * @param {number} reward - Episode reward
     * @param {number} lines - Lines cleared in episode
     */
    addDataPoint(episode, reward, lines) {
        this.chart.data.labels.push(episode);
        this.chart.data.datasets[0].data.push(reward);
        this.chart.data.datasets[1].data.push(lines);

        // Remove old points if exceeded max
        if (this.chart.data.labels.length > this.maxPoints) {
            this.chart.data.labels.shift();
            this.chart.data.datasets[0].data.shift();
            this.chart.data.datasets[1].data.shift();
        }

        this.chart.update('none');  // 'none' skips animation
    }

    /**
     * Clear all chart data
     */
    clear() {
        this.chart.data.labels = [];
        this.chart.data.datasets[0].data = [];
        this.chart.data.datasets[1].data = [];
        this.chart.update();
    }
}
```

3. `src/web/static/app.js` - Main application (stub for now, Plan 04 completes):
```javascript
/**
 * ML Tetris Trainer - Main Application
 * Coordinates WebSocket, game board, and metrics chart
 */

// Initialize components when DOM ready
document.addEventListener('DOMContentLoaded', () => {
    console.log('ML Tetris Trainer initializing...');

    // Initialize game board
    const gameBoard = new GameBoard('game-board');
    gameBoard.clear();

    // Initialize metrics chart
    const metricsChart = new MetricsChart('metrics-chart');

    // Store references globally for debugging
    window.gameBoard = gameBoard;
    window.metricsChart = metricsChart;

    // UI Elements
    const btnStart = document.getElementById('btn-start');
    const btnStop = document.getElementById('btn-stop');
    const statusIndicator = document.getElementById('status-indicator');
    const statusText = document.getElementById('status-text');

    // Placeholder click handlers (WebSocket integration in Plan 04)
    btnStart.addEventListener('click', () => {
        console.log('Start clicked - WebSocket integration pending');
        // TODO: Send start command via WebSocket
    });

    btnStop.addEventListener('click', () => {
        console.log('Stop clicked - WebSocket integration pending');
        // TODO: Send stop command via WebSocket
    });

    console.log('ML Tetris Trainer ready (WebSocket pending)');
});

/**
 * Update UI elements with metrics data
 * @param {object} data - Metrics data from WebSocket
 */
function updateMetrics(data) {
    if (data.episode_count !== undefined) {
        document.getElementById('episode-count').textContent = data.episode_count;
    }
    if (data.current_score !== undefined) {
        document.getElementById('current-score').textContent = data.current_score;
    }
    if (data.lines_cleared !== undefined) {
        document.getElementById('lines-cleared').textContent = data.lines_cleared;
    }
    if (data.timesteps !== undefined) {
        document.getElementById('timesteps').textContent = data.timesteps.toLocaleString();
    }
    if (data.avg_reward !== undefined) {
        document.getElementById('avg-reward').textContent = data.avg_reward.toFixed(2);
    }
    if (data.best_lines !== undefined) {
        document.getElementById('best-lines').textContent = data.best_lines;
    }
}

/**
 * Update training status indicator
 * @param {string} status - "stopped", "running", or "paused"
 */
function updateStatus(status) {
    const indicator = document.getElementById('status-indicator');
    const text = document.getElementById('status-text');
    const btnStart = document.getElementById('btn-start');
    const btnStop = document.getElementById('btn-stop');

    indicator.className = 'status ' + status;
    text.textContent = status.charAt(0).toUpperCase() + status.slice(1);

    btnStart.disabled = (status === 'running');
    btnStop.disabled = (status !== 'running');
}
```
  </action>
  <verify>
    ```bash
    test -f src/web/static/game-board.js && test -f src/web/static/metrics-chart.js && test -f src/web/static/app.js && echo "JS files OK"
    grep -q "class GameBoard" src/web/static/game-board.js && grep -q "class MetricsChart" src/web/static/metrics-chart.js && echo "Classes defined"
    ```
  </verify>
  <done>
    JavaScript modules created: GameBoard for Canvas rendering, MetricsChart for Chart.js, app.js for coordination (WebSocket pending).
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
1. index.html renders in browser with split view layout
2. CSS provides Tetris-themed dark styling
3. JavaScript files load without console errors
4. GameBoard renders empty grid, MetricsChart shows empty chart
</verification>

<success_criteria>
- VIS-01: Web interface created with proper HTML structure
- VIS-03: Status indicator implemented (running/paused/stopped states)
- VIS-04: Split view layout with game board and metrics panels
- TRAIN-05: Progress display elements (episode, score, lines)
- TRAIN-06: Chart.js chart ready for real-time updates
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-visualization/03-02-SUMMARY.md`
</output>
